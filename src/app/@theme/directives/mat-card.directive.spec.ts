import { MatCardDirective } from './mat-card.directive';

describe('MatCardDirective', () => {
  it('should create an instance', () => {
    const directive = new MatCardDirective();
    expect(directive).toBeTruthy();
  });
});
