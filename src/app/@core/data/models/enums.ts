export enum TourStatus { Create = 1, Running = 2, End = 3}

export enum TourType {Flight = 1, Earth = 2}

export  enum ServiceType {Bus = 1, Bedroom = 2, Food = 4}

export type HttpMethod = "GET" | "POST" | "PUT";
